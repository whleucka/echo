; Development overrides (loaded after opcache.ini)
display_errors=On
memory_limit=512M
upload_max_filesize=64M
post_max_size=64M

; Re-enable timestamp validation for development so file changes
; are picked up without restarting FPM
opcache.validate_timestamps=1
opcache.revalidate_freq=0
